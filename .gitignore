node_modules/
dist/
*.js
*.js.map
*.d.ts
!jest.config.js
!jest.config.ts
agent-mesh.json
.omo/
out-receipted-fleet/
scratch/
# Session worktrees for multi-commit work. The tree itself must never be
# committed — a node_modules symlink inside one was committed once.
.wt-meshfleet/

# Local agent-session instructions, review patches, and working plans are not
# part of the public Core surface.
.claude/
.superpowers/
docs/superpowers/

# Python reference-witness bytecode (reference/python/)
__pycache__/
*.pyc

# Merge-train local artefacts. The merge-train script and its portfolio
# wrapper are tracked under scripts/, scripts/lib/, and test/, but the
# per-run dossier + the operator's portable repo-list config are
# machine-local (the dossier references absolute paths and the config
# almost certainly will too).
MERGE-TRAIN-*.md
MERGE-TRAIN-PORTFOLIO-*.md
.portfolio-merge-train.json
heavy-build
heavy-build.d/
