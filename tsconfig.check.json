{
  "//": "Typecheck-only config. `tsconfig.json` is the BUILD config and its `include` is",
  "//1": "`src/**/*` only, so `tsc --noEmit` against it never loaded `scripts/` or `test/` — a type",
  "//2": "error there was invisible to every stage of the verifier (tests run under `tsx`, which",
  "//3": "strips types without checking them). This widens the net without changing what",
  "//4": "`npm run build` emits: it is `noEmit`, and the build still uses `tsconfig.json`.",
  "//5": "",
  "//6": "`test/**/*` was added only once its errors were genuinely fixed — 64 of them, none by an",
  "//7": "ignore list and none by changing what a test feeds. The deliberately malformed fixtures",
  "//8": "kept their exact values and were widened with `as unknown as X`. Do NOT reach for",
  "//9": "`exclude`, `skipLibCheck` scope tricks, or `@ts-expect-error` when this stage goes red:",
  "//10": "a finding downgraded to keep a pipeline green is the failure this repo exists to prevent.",
  "//11": "To measure coverage rather than trust this comment, run",
  "//12": "`tsc -p tsconfig.check.json --listFiles` and look for the directory you care about —",
  "//13": "which is exactly what `test/typecheck-coverage.test.ts` does on every run.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["src/**/*", "scripts/**/*", "test/**/*"]
}
